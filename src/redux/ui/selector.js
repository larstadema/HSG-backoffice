export const errors = (state) => state.UI.errors

export const isLoading = (state) => state.UI.loading

export const isOpen = (state) => state.UI.open

export const isDarkTheme = (state) => state.UI.dark
