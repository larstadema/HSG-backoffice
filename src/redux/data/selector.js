export const reservations = (state) => state.data.reservations

export const oldReservations = (state) => state.data.oldReservations

export const dates = (state) => state.data.dates

export const events = (state) => state.data.events

export const isLoading = (state) => state.data.loading
